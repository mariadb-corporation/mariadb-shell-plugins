; Copyright (c) 2026, MariaDB plc.
[pytest]
junit_family = xunit1
markers =
    e2e: end-to-end test that deploys its own servers and reaches the network (a server package download, or the migration tooling install). Skipped unless --e2e is given.
addopts = --color=yes -ra --doctest-glob="*.dt" --junitxml=plugin-tests.xml --cov-report term --cov-report xml:plugin-test-coverage.xml --cov-report html --cov-config=".coveragerc"
